---
title: "Model"
format: pdf
editor: visual
---
```{r}
library(tidyverse)
```

```{r}
data_train <- read.csv("data-train.csv")
data_test <- read.csv("data-test.csv")
```

##Data Cleaning
```{r}
# Change Fr and Re to categorical
data_train$Fr <- as.factor(data_train$Fr)
data_train$Re <- as.factor(data_train$Re)

```

##Linear Model
```{r}
lm.fit <- lm(cbind(R_moment_1, R_moment_2, R_moment_3, R_moment_4) ~ St + Re + Fr, data = data_train)
summary(lm.fit)
```
```{r}
data_train <- data_train |>
  mutate(
    log_m2 = log(R_moment_2),
    log_m3 = log(R_moment_3),
    log_m4 = log(R_moment_4)
  )
```


```{r}
lm2 <- lm(R_moment_2 ~ poly(St, 2) + poly(Re, 2) + Fr, data = data_train)
summary(lm2)
```
```{r}
lm3 <- lm(R_moment_2 ~ (St + Re + Fr)^2, data = data_train)
summary(lm3)
```

